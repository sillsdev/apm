/** Default auto-segment parameters for the transcriber step (also used for clause-moves). */
export const transcribeDefaultSegParams = {
  silenceThreshold: 0.004,
  timeThreshold: 0.02,
  segLenThreshold: 0.5,
};
