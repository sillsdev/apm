import PassageDetailMarkVerses, {
  MarkVersesProps,
} from '../../PassageDetailMarkVerses';

const PassageDetailMarkVersesIsMobile = ({ width }: MarkVersesProps) => (
  <PassageDetailMarkVerses width={width} />
);

export default PassageDetailMarkVersesIsMobile;
